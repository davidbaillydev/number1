[build]
  publish = "meghna-hugo/public/index.html"
  command = "cd meghna-hugo && hugo --gc --themesDir ../.."
  
[build.environment]
  HUGO_VERSION = "0.79.0"
  HUGO_THEME = "repo"
  HUGO_BASEURL = "/"
